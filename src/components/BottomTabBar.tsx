import { useNavigate, useLocation } from "react-router-dom";
import { Home, Dashboard, Plus, Calendar, Users } from "lucide-react"; // Fixed: Dashboard is now available

const BottomTabBar = () => {
  // ... rest of the component
};

export default BottomTabBar;